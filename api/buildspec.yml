version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: 2.2
  build:
    commands:
      - cd api
      - dotnet publish -o deploy
artifacts:
  files:
    - applicationStop.bat
    - applicationStart.bat
    - appspec.yml
    - api/deploy/*
