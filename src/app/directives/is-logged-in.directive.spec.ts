import { IsLoggedInDirective } from './is-logged-in.directive';

describe('IsLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
